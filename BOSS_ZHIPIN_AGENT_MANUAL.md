# BOSS直聘 Agent 操作手册

更新时间：2026-03-13

适用范围：
- PC 网页端
- 以“求职者找岗位”为主
- 面向需要通过“观察页面反馈 -> 修正操作 -> 形成稳定工作流”的 agent / RPA / 半自动桌面代理

标记说明：
- `[官方]` 来自 BOSS 直聘官方页面或官方帮助中心
- `[实测]` 来自 2026-03-13 在网页端的实际操作观察
- `[推断]` 来自官方信息与实测行为的组合推断

## 1. 核心思路

如果把 BOSS 直聘看作一个要“学会使用”的系统，最有效的方式不是死记像素坐标，而是建立一套“动作 -> 反馈 -> 含义 -> 下一步”的映射表。

推荐优先级：

1. 已知公开 URL，直接跳转 URL
2. URL 无法直接表达当前目标，再做页面点击
3. 每次动作后都观察反馈，再决定下一步
4. 对高频稳定动作，沉淀成固定流程

对 agent 最重要的一点：

- 只要某个点击动作本质上等价于“进入一个公开网页路由”，优先直接打开该 URL，而不是重新点击页面

## 2. 站点结构总览

### 2.1 首页

- `https://www.zhipin.com/` `[官方]`
- 首页底部可见“职位搜索 / 使用帮助 / 防骗指南 / 协议与规则”等入口 `[官方]`

### 2.2 职位搜索结果页

实测稳定可用的网页端搜索路由：

```text
https://www.zhipin.com/web/geek/jobs?city=<CITY_CODE>&query=<KEYWORD>
```

示例：

```text
https://www.zhipin.com/web/geek/jobs?city=101210100&query=agent%E5%BC%80%E5%8F%91
```

说明：

- `city` 是城市代码 `[实测]`
- `query` 是搜索关键词，建议做 URL 编码 `[实测]`
- `city=101210100` 对应杭州，是网页端搜索结果页当前可用的杭州代码 `[实测]`

### 2.3 职位详情页

实测存在两类详情页路由：

```text
https://www.zhipin.com/job_detail/<JOB_ID>.html
https://activity.zhipin.com/job_detail/<JOB_ID>.html
```

说明：

- 第一类是主站职位详情页 `[实测]`
- 第二类是活动域名下的职位详情页 `[实测]`
- 未通过验证、未登录或触发风控时，直接打开详情页可能被重定向到安全校验页 `[实测]`

### 2.4 公司页

实测存在两类公司主页路由：

```text
https://www.zhipin.com/companys/<COMPANY_ID>.html
https://www.zhipin.com/gongsi/<COMPANY_ID>.html
```

说明：

- 两类路径在当前站点中都真实存在 `[官方][实测]`
- 公司页通常包含：公司简介、在招职位、人员规模、融资阶段、热门企业等 `[官方]`

### 2.5 安全校验页

实测会遇到两类安全页：

```text
https://www.zhipin.com/web/common/security-check.html?callbackUrl=...
https://www.zhipin.com/web/user/safe/verify-slider?callbackUrl=...
```

含义：

- 站点要求进行安全校验、滑块或登录相关验证 `[实测]`
- 对 agent 而言，这意味着“当前动作需要人工接管或换成已登录浏览器会话” `[推断]`

## 3. 页面模型

搜索结果页可以抽象成 4 个区域：

1. 顶部搜索区
2. 条件筛选区
3. 左侧岗位列表
4. 右侧当前岗位详情

对 agent 最关键的不是页面长什么样，而是每个区域承担什么职责。

### 3.1 顶部搜索区

用途：

- 输入关键词
- 触发新的结果页

agent 友好策略：

- 优先直接改 URL 的 `query` 参数，而不是点击输入框后逐字输入 `[实测][推断]`

### 3.2 条件筛选区

常见筛选项：

- 工作区域
- 职位类型
- 求职类型
- 薪资待遇
- 工作经验
- 学历要求
- 公司行业
- 公司规模
- 融资阶段

agent 友好策略：

- `city + query` 适合用 URL 直达
- 其他筛选项多数不稳定地编码在当前可见 URL 中，优先通过页面筛选控件操作 `[实测][推断]`

### 3.3 左侧岗位列表

作用：

- 提供当前搜索批次的岗位入口

反馈特征：

- 当前选中岗位通常会在左侧卡片上出现高亮边框 `[实测]`
- 切换卡片后，右侧详情会更新 `[实测]`

### 3.4 右侧岗位详情

常见信息：

- 岗位名称
- 薪资
- 城市 / 区域
- 学历 / 经验
- 职位描述
- 公司信息
- 收藏按钮
- 立即沟通按钮

对 agent 的价值：

- 这里是“是否符合意向”的最终判断区
- 也是“收藏 / 直聊 / 跳公司页”的执行区

### 3.5 职位结果页关键坐标补充（2026-03-14 实测）

以下坐标均基于 `agent-computer` 绘制的网格坐标系，也就是屏幕绝对坐标 `[实测]`。

适用页面：

```text
https://www.zhipin.com/web/geek/jobs
```

#### 3.5.1 顶部 header

从左到右依次包含：

- BOSS直聘 logo
- 首页：`(410, 185)`
- 职位（当前界面）：`(470, 185)`
- 公司：`(530, 185)`
- 校园：`(600, 185)`
- 海归：`(650, 185)`
- APP：`(710, 185)`
- 有了：`(770, 185)`
- 海外：`(825, 185)`
- 无障碍专区：`(900, 185)`
- 消息：`(1460, 185)`，点击进入我的消息
- 简历：`(1525, 185)`，点击进入我的简历
- 个人界面：`(1600, 185)`，`hover` 之后出现下拉选项

#### 3.5.2 左侧岗位列表

- 左边是岗位列表，每个列表项是一个卡片，可滚动 `[实测]`
- 卡片会展示岗位基本信息 `[实测]`
- 点击卡片的公司名称时，会进入公司的详情界面 `[实测]`
- 点击卡片其他非公司名称区域时，会进入岗位详情界面 `[实测]`

#### 3.5.3 右侧岗位详情按钮

- 收藏按钮坐标：`(1425, 460)` `[实测]`
- 立即沟通按钮坐标：`(1575, 460)` `[实测]`

## 4. 反馈驱动学习映射

这是本手册最重要的部分。

### 4.1 搜索动作

动作：

- 打开 `https://www.zhipin.com/web/geek/jobs?city=<CITY_CODE>&query=<KEYWORD>`

预期反馈：

- 左侧出现一批岗位卡片
- 右侧出现一个默认选中岗位的详情

含义：

- 搜索成功，进入结果页

下一步：

- 先看默认选中岗位是否符合意向
- 再决定是否调整筛选或开始滚动

### 4.2 点击筛选项

动作：

- 点击“融资阶段 / 公司规模 / 职位类型”等筛选 pill

预期反馈：

- 页面上方弹出一个下拉选项层 `[实测]`

含义：

- 当前进入筛选选择态

下一步：

- 选择最能提高命中率的条件
- 例如优先选择“已上市”来提高大厂密度 `[实测][推断]`

### 4.3 点击左侧岗位卡片

动作：

- 点击左侧列表中的某一条岗位

预期反馈：

- 左侧高亮边框切换
- 右侧岗位详情更新

含义：

- 当前上下文从“岗位 A”切换成“岗位 B”

下一步：

- 只在右侧详情判断是否符合你的意向，不要只看标题就决定

### 4.4 点击收藏

动作：

- 点击右侧“收藏”

预期反馈：

- 按钮文案切换为“取消收藏” `[实测]`

含义：

- 当前岗位已被收藏

下一步：

- 记录已收藏
- 继续滚动或切换岗位

### 4.5 按 `PageDown`

动作：

- 在结果页按 `PageDown`

预期反馈：

- 页面整体下滚
- 左侧岗位列表推进到下一批可见岗位 `[实测]`
- 有时右侧详情会保持当前选中岗位，有时会随着当前可见主卡片变化而变化 `[实测]`

含义：

- `PageDown` 是当前比滚轮更稳定的“批量推进”动作

下一步：

- 每次 `PageDown` 后都要重新看左侧第一条和右侧详情，不要假设选中态一定同步变化

### 4.6 鼠标滚轮

动作：

- 在搜索结果页使用滚轮

预期反馈：

- 可能滚动整个页面
- 也可能只滚动右侧详情 pane `[实测]`

含义：

- 滚轮对页面焦点位置敏感，不如 `PageDown` 稳定

下一步：

- 需要批量翻页时优先用 `PageDown`
- 需要精调详情时再用滚轮

## 5. Agent 友好操作优先级

### 5.1 优先直跳 URL 的操作

这些动作本质是“进入一个公开页面”，优先 URL：

1. 进入首页
2. 进入某城市 + 某关键词的搜索结果页
3. 打开已知公司页
4. 打开已知职位详情页

推荐直接跳：

```text
/web/geek/jobs?city=...&query=...
/companys/<id>.html
/gongsi/<id>.html
/job_detail/<id>.html
```

### 5.2 不建议依赖点击的场景

这些场景点击容易浪费时间：

- 从首页点击搜索框再输关键词
- 为了进职位详情而反复点卡片标题
- 为了进公司页而先点职位再找公司名

更优做法：

- 已知路由就直跳
- 未知路由时，点击一次并观察浏览器 URL 或页面反馈，再沉淀成固定模式

### 5.3 必须保留点击的场景

这些动作通常不能稳定用 URL 表达：

- 融资阶段筛选
- 公司规模筛选
- 收藏 / 取消收藏
- 立即沟通
- 下拉菜单中的选项选择

## 6. 面向“找符合意向岗位”的判断规则

你的意向：

1. 不做纯 Java
2. 可以做 agent + Java
3. 喜欢做纯 agent
4. 大厂
5. 硬条件是杭州

因此对 agent 的判断规则建议写成：

### 6.1 直接通过

- 标题含 `AI Agent`
- 标题含 `Agent平台`
- 描述中出现 `Multi-Agent / Agent / Prompt Engineering / RAG / Tool Calling / Memory / Planning / Execution`
- 公司信息明确是已上市、大厂、头部互联网、头部平台
- 地点为杭州

### 6.2 条件通过

- 纯 Java 不通过
- `Agent + Java` 通过
- 后端平台岗如果描述里明确包含 Agent 能力、LLM 接入、RAG、工具调用，则通过

### 6.3 直接跳过

- 描述主体是纯 Java CRUD / 纯 Spring 微服务而无 Agent、LLM、RAG、Prompt、工具链痕迹
- 明显销售、运营、招生、客服
- 非杭州
- 公司明显非大厂且没有特别强的 Agent 技术含量

## 7. 推荐工作流

### 7.1 初始化

1. 打开杭州搜索结果页
2. 关键词设为 `agent开发`
3. 若希望提高大厂密度，先尝试 `融资阶段=已上市`
4. 确保浏览器已最大化

### 7.2 单轮循环

1. 看右侧当前详情
2. 判断是否满足“杭州 + 大厂 + 非纯 Java + Agent/Agent+Java”
3. 若满足，点收藏，直到按钮变成“取消收藏”
4. 按 `PageDown`
5. 重新观察新的左侧主卡片与右侧详情
6. 重复

### 7.3 卡片切换策略

优先级：

1. 如果右侧当前卡片已经符合，直接收藏
2. 如果左侧第一条不合适，再尝试切第二条
3. 若卡片切换不稳定，不要在同一屏反复试错太久，直接 `PageDown`

这是因为：

- 对 agent 来说，稳定推进比在单屏内追求全收更重要 `[推断]`

## 8. 反脆弱规则

### 8.1 一旦命中安全页，立即切换策略

如果进入以下路由：

- `/web/common/security-check.html`
- `/web/user/safe/verify-slider`

说明：

- 当前浏览器会话、请求方式或访问频率触发了风控 `[实测]`

处理原则：

1. 不要继续高频重试
2. 改用已经登录且当前正在使用的浏览器窗口
3. 必要时人工完成一次验证，再继续

### 8.2 不要默认滚轮一定滚整个页面

因为右侧详情 pane 会抢滚动焦点 `[实测]`

处理原则：

- 批量推进优先 `PageDown`
- 详情精调再用滚轮

### 8.3 每次只确认一个稳定反馈

错误做法：

- 连续点击多个控件再回头看结果

正确做法：

1. 做一个动作
2. 看一个反馈
3. 再决定下一步

## 9. 适合写入 agent 的规则摘要

可以直接塞进自动化系统的规则：

```text
1. 优先 URL 直达，不优先手点搜索框。
2. 搜索结果页主路由固定为 /web/geek/jobs?city=<code>&query=<kw>。
3. 批量推进优先 PageDown，不优先滚轮。
4. 右侧详情是最终判断区，不要只看左侧标题。
5. 收藏成功的唯一可靠反馈是“收藏”变成“取消收藏”。
6. 命中 /security-check 或 /verify-slider 即视为需要人工接管。
7. 公司页优先 /companys/<id>.html，兼容 /gongsi/<id>.html。
8. 职位详情优先 /job_detail/<id>.html；若已知详情 URL，优先直跳。
9. 对你的意向，纯 Java 直接跳过；Agent + Java 可过；纯 Agent 优先。
10. 杭州是硬条件，不满足直接跳过。
```

## 10. 视频演示版求职者操作全指南

下面这一节是按你提供的“绝对正确”操作手册完整并入，尽量不改动原始信息，以免丢失视频演示中的求职者使用动线。

# BOSS直聘网页版求职者操作全指南

## 模块一：基础设置与界面导航

在开始求职前，熟悉网站的基本布局和基础设置可以事半功倍。

1. **城市定位与切换**
   - **操作**：在搜索栏左侧或网站左上角，点击当前的城市名称（如“杭州”或“阜阳”）。
   - **功能**：会弹出一个城市选择面板，支持按热门城市、首字母排序或直接输入城市名称进行搜索切换，确保您看到的职位都是目标城市的。

2. **顶部导航栏**
   - 包含 **首页、职位、公司、消息、言职** 等核心频道，方便您在找工作、查公司资料和回复消息之间快速切换。

3. **个人账号管理**
   - **操作**：将鼠标悬停在右上角的“个人头像”处。
   - **功能**：下拉菜单中可进行 **升级VIP、进入个人中心、简历中心（编辑/查看简历）、隐私保护** 以及 **切换为招聘者** 等账号核心管理操作。

## 模块二：发现与搜索职位

BOSS直聘提供了多种发现匹配职位的方式，从泛读到精准搜索。

1. **利用首页分类导航（适合随便看看 / 了解行情）**
   - 首页左侧拥有详细的行业和岗位分类树（如：互联网/AI、电子/通信、产品、运营等）。将鼠标悬停在主分类上，右侧会展开具体的细分岗位，点击即可进入该类别的职位列表。

2. **首页推荐信息流（适合捡漏）**
   - 页面向下滑动，系统会根据您的简历和搜索习惯，展示 **“精选职位”**、**“最新职位”**、**“热招职位”** 以及 **“热门企业”**。

3. **精准关键词搜索（最常用功能）**
   - **操作**：在顶部巨大的搜索框内输入您的目标岗位关键词（视频中演示输入了“agent开发”），然后点击右侧的 **“搜索”** 按钮。

## 模块三：高级条件筛选（精准定位）

进入搜索结果页后，为了在海量职位中找到最符合自己预期的工作，必须使用上方的 **高级筛选栏**。

- **行政区划 / 商圈**：可具体到城市下的某个区（如杭州的余杭区、西湖区）甚至特定商圈。
- **职位类型**：可筛选全职、兼职或 **实习生**（视频中特意勾选了实习生）。
- **薪资待遇**：设置您的期望底线和上限（如：200-250元/天，或特定月薪范围）。
- **其他核心筛选条件**：还可以依次点开 **工作经验**、**学历要求**、**公司行业**、**公司规模**、**融资阶段**（如：未融资、A轮、已上市等）进行勾选，系统会实时刷新下方的职位列表。

## 模块四：职位查看与评估

经过筛选后，页面呈现 **左侧列表 + 右侧详情** 的双栏布局，浏览体验极佳。

1. **左侧列表速览**
   - 快速查看岗位名称、薪资、公司名称、融资情况以及HR的姓名和活跃状态。

2. **右侧详情精读（Job Description）**
   - **操作**：点击左侧任意一个感兴趣的职位，右侧会自动加载该职位的详细信息。
   - **核心关注点**：
     - **职位描述**：详细阅读岗位职责和任职要求（What you will do & What we need）。
     - **工作地址**：查看具体的上班地点及地图定位。
     - **公司信息**：了解公司背景。
   - **收藏职位**：如果暂时不想沟通但觉得不错，可以点击右上角的 **“☆ 收藏”** 按钮备用。

## 模块五：发起沟通与投递

BOSS直聘的核心特色是“直接与老板/HR开聊”，当您看中某个职位时，即可发起沟通。

1. **点击“立即沟通”**
   - 在右侧职位详情页的右上角，点击绿色的 **“立即沟通”** 按钮。

2. **确认发送打招呼语**
   - 点击后，系统通常会弹出一个确认窗口（提示将发送您的微简历和默认打招呼语），点击 **“确定”或“发送”**。

3. **进入聊天界面（消息中心）**
   - 确认后，页面会自动跳转到“消息”聊天窗口（类似微信网页版界面）。
   - 在这里，您可以与HR进行实时文字对话，发送完整版附件简历，询问面试流程等。

4. **返回继续寻找**
   - 沟通完毕后，可以直接点击浏览器后退，或再次点击顶部的“搜索”栏，回到刚才的职位列表，重复上述步骤（视频中演示了连续向多个相似岗位发起沟通的过程）。

### 求职小贴士（基于演示操作的延伸建议）

- **批量沟通**：像视频中一样，利用好筛选功能后，可以快速浏览并对多个符合条件的岗位点击“立即沟通”，提高获得面试机会的概率。
- **完善简历**：在大量点击“立即沟通”前，请务必通过右上角头像进入“简历中心”，确保您的在线微简历和附件简历已经更新到最佳状态，因为这是HR收到的第一块“敲门砖”。

## 11. 公开入口与参考链接

官方入口：

- 首页：<https://www.zhipin.com/>
- 使用帮助：<https://www.zhipin.com/web/common/protocol/use-help.html>
- 防骗指南：<https://news.zhipin.com/web/common/protocol/cheat-guide.html>
- PC 端常见问题：<https://www.zhipin.com/desktop/faq/>

公开路由样例：

- 搜索结果页：<https://www.zhipin.com/web/geek/jobs?city=101210100&query=agent%E5%BC%80%E5%8F%91>
- 公司页样例一：<https://www.zhipin.com/companys/074ff7b3eb1a94a903Z62dy4.html>
- 公司页样例二：<https://www.zhipin.com/gongsi/5d627415a46b4a750nJ9.html>
- 职位详情页样例：<https://www.zhipin.com/job_detail/b34bf1e030cc298103V_0t68FVRX.html>

## 12. 来源说明

本手册结合了 3 类信息：

1. 官方帮助与官网页 `[官方]`
2. 2026-03-13 对网页端公开路由与安全跳转的实时访问观察 `[实测]`
3. 在桌面端实际操作 BOSS 搜索页时总结出的动作-反馈映射 `[实测][推断]`

如果后续网站 UI 改版，优先相信这份手册中的“结构规律”而不是某个具体像素点：

- URL 优先
- 反馈驱动
- 单步确认
- 逐步沉淀稳定映射
